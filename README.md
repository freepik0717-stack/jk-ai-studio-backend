# JK AI STUDIO - Backend (Demo)

This is a simple demo backend for JK AI STUDIO. It exposes one endpoint:

POST /generate
Body: { "title": "Your Title", "style": "real" | "cartoon" }
Response: { "videoUrl": "https://...", "script": "..." }

Used for demo/testing. Replace with real AI integrations when ready.
